// In rules.mk
TAP_DANCE_ENABLE = yes

// In keymap.c
#include "quantum.h"

enum tap_dance_actions {
    TD_LAYER_CYCLE,  // single: nothing, double: cycle layer
};

tap_dance_action_t tap_dance_actions[] = {
    [TD_LAYER_CYCLE] = ACTION_TAP_DANCE_DOUBLE(KC_A, KC_B),
};

// Then use TD(TD_LAYER_CYCLE) in your keymap layout





more complex

void td_layer_finished(tap_dance_state_t *state, void *user_data) {
    if (state->count == 1) {
        tap_code(KC_A);
    } else if (state->count == 2) {
        layer_move(next_layer(active_layer()));
    }
}

tap_dance_action_t tap_dance_actions[] = {
    [TD_LAYER_CYCLE] = ACTION_TAP_DANCE_FN_ADVANCED(NULL, td_layer_finished, NULL),
};
